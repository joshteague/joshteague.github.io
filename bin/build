#!/usr/bin/env bash
# One-shot build into _site/ — same output GitHub Pages will produce.
set -euo pipefail
cd "$(dirname "$0")/.."
export LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8
exec bundle exec jekyll build "$@"
