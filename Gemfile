source "https://rubygems.org"

# Mirrors what GitHub Pages actually runs, so local previews match production.
# https://pages.github.com/versions/
gem "github-pages", "~> 228", group: :jekyll_plugins

# Last ffi line that still supports the macOS system Ruby (2.6).
gem "ffi", "< 1.17"
